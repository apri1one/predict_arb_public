var Preview = window.Preview || (window.Preview = {});
var { useState, useEffect, useMemo, useRef, useCallback } = React;

Preview.ReactHooks = {
    useState,
    useEffect,
    useMemo,
    useRef,
    useCallback,
};
